<header class="bg-white shadow flex justify-between items-center px-4 py-6">
        {{#if user}}
            <a class="navbar-brand" href="/user">
                <img src="/images/logo.png" width="100" height="50" alt="Logo">    
            </a>    
        {{else}}
            <a class="navbar-brand" href="/login">
                <img src="/images/logo.png" width="100" height="50" alt="Logo">    
            </a>
        {{/if}}
   
    <div class="flex items-center">
        {{#if user}}
        <input id="search-input" type="text" placeholder="Search products"
            class="border border-gray-300 rounded-md px-4 py-2 focus:outline-none focus:border-blue-500">
        <a href="#" class="text-gray-600 hover:text-gray-800 mx-4">Products</a>
        <a href="#" class="text-gray-600 hover:text-gray-800 mx-4">Profile</a>
        <a href="#" class="text-gray-600 hover:text-gray-800 mx-4">Cart</a>
        <form action="/logout" method="post">
            <button>logout</button>
        </form>
        {{else}}
            <a href="/login" class="text-gray-600 hover:text-gray-800 mx-4">Login</a>
            <a href="/register" class="text-gray-600 hover:text-gray-800 mx-4">Register</a>
        {{/if}}
    </div>
</header>